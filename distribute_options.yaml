output: dist/
releases:
  - name: release
    jobs:
      # Build and publish your apk pkg to pgyer
      - name: release-linux
        package:
          platform: linux
          target: deb
          build_args:
            target-platform: linux
        publish_to: github
     